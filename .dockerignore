.git
.github
.claude
vendor
node_modules
templates/_compiled
templates/_cache
# dev-config.php is committed and must ship: config.php includes it whenever
# ENVIRONMENT=STAGING. Excluding it makes the staging image fatal on the
# undefined *_SCRIPT_LOCATION constants. Production ignores the file entirely.
*.md
